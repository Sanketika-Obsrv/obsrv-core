<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
				 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
				 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.sunbird.obsrv</groupId>
	<artifactId>core</artifactId>
	<version>1.0</version>
	<packaging>pom</packaging>
	<name>Obsrv Core</name>
	<description>
		Obsrv Core
	</description>
	<url>https://sunbird.org/</url>

	<modules>
		<module>framework</module>
		<module>dataset-registry</module>
		<module>transformation-sdk</module>
		<module>pipeline</module>
		<module>data-products</module>
	</modules>

	<properties>
		<!-- maven specific properties -->
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<scala.maj.version>2.12</scala.maj.version>
		<!-- 2.12.18, not 2.12.11: matches main's own scala.version (bd579898). This is a
		     patch/bugfix bump within Scala 2.12, binary-compatible for every module - not
		     related to the flink.version bump below, which stays untouched here since it's
		     still out of scope for modules other than framework/hudi-connector on this
		     branch (see framework/pom.xml and pipeline/hudi-connector/pom.xml for those). -->
		<scala.version>2.12.18</scala.version>
		<flink.version>1.17.2</flink.version>
		<kafka.version>3.7.1</kafka.version>
		<java.target.runtime>11</java.target.runtime>
		<jackson-jaxrs.version>1.9.13</jackson-jaxrs.version>
		<scoverage.plugin.version>1.4.0</scoverage.plugin.version>
	</properties>

	<repositories>
		<repository>
			<id>my-local-repo</id>
			<url>file://${user.home}/.m2/repository</url>
		</repository>
		<repository>
			<id>apache-releases</id>
			<url>https://repository.apache.org/content/groups/public</url>
		</repository>
	</repositories>
	<!-- scala-tools.org / oss.sonatype.org/content/groups/scala-tools <repository> and
	     <pluginRepository> entries removed - the URL 404s (verified), dead for years, and
	     nothing in this reactor actually needs it. -->

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.8.1</version>
					<configuration>
						<source>${java.target.runtime}</source>
						<target>${java.target.runtime}</target>
					</configuration>
				</plugin>
				<plugin>
				<groupId>org.scoverage</groupId>
					<artifactId>scoverage-maven-plugin</artifactId>
					<version>${scoverage.plugin.version}</version>
					<configuration>
						<scalaVersion>${scala.version}</scalaVersion>
						<aggregate>true</aggregate>
						<highlighting>true</highlighting>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
</project>
